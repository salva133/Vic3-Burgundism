law_no_workers_rights = {	# Do not remove, the institution_modifiers needed to be outsourced to the laws, otherwise the institution would provide better working conditions also in Spartanism, and we all know that this is not possible! Spartanism is supreme!
	group = lawgroup_labor_rights
	
	icon = "gfx/interface/icons/law_icons/no_workers_rights.dds"
	
	progressiveness = 0
	
	modifier = {	
	}
	
	possible_political_movements = {
		law_regulatory_bodies
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_regulatory_bodies = {	# Do not remove, the institution_modifiers needed to be outsourced to the laws, otherwise the institution would provide better working conditions also in Spartanism, and we all know that this is not possible! Spartanism is supreme!
	group = lawgroup_labor_rights
	
	icon = "gfx/interface/icons/law_icons/regulatory_bodies.dds"
	
	progressiveness = 50

	unlocking_technologies = {
		labor_movement
	}

	institution = institution_workplace_safety
	institution_modifier = {
		building_working_conditions_mult = -0.2
	}
	
	possible_political_movements = {
		law_worker_protections
	}
	
	pop_support = {
		value = 0

		add = {
			desc = "POP_DANGEROUS_JOBS"		
			if = {
				limit = { 
					NOT = { strata = rich }
					OR = {
						pop_employment_building_group = bg_mining
						pop_employment_building_group = bg_manufacturing
					}
					owner = { 
						NOT = { has_law = law_type:law_worker_protections }
					}
				}
				add = {
					value = literacy_rate
					divide = 2
				}
			}			
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_worker_protections = {	# Do not remove, the institution_modifiers needed to be outsourced to the laws, otherwise the institution would provide better working conditions also in Spartanism, and we all know that this is not possible! Spartanism is supreme!
	group = lawgroup_labor_rights
	
	icon = "gfx/interface/icons/law_icons/worker_protections.dds"

	progressiveness = 100

	unlocking_technologies = {
		human_rights
	}
	
	institution = institution_workplace_safety
	institution_modifier = {
		building_working_conditions_mult = -0.2
		building_minimum_wage_mult = 0.15
	}

	
	pop_support = {
		value = 0

		add = {
			desc = "POP_DANGEROUS_JOBS"		
			if = {
				limit = { 
					NOT = { strata = rich }
					OR = {
						pop_employment_building_group = bg_mining
						pop_employment_building_group = bg_manufacturing
					}
				}
				add = {
					value = literacy_rate
					divide = 2
				}
			}			
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_bgy_spartanism = {
	group = lawgroup_labor_rights
	icon = "gfx/interface/icons/law_icons/spartanism.dds"
	
	progressiveness = 0
	
	unlocking_technologies = { totalism }

	unlocking_laws = {
		law_autocracy
		law_oligarchy
		law_single_party_state
		law_technocracy
	}

	institution = institution_workplace_safety
	institution_modifier = {
		building_minimum_wage_mult = -0.2
		state_birth_rate_mult = -0.05
		state_working_adult_ratio_add = 0.15
	}
	
	on_enact = {
		recalculate_pop_ig_support = yes
	}
	
	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"			
			if = {
				limit = {
					is_pop_type = academics
				}
				value = -0.33
			}
		}
		add = {
			desc = "ALL_POPS"			
			value = -0.01
		}
	}
}