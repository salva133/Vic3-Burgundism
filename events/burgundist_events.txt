namespace = burgundism

burgundism.1 = {
	type = country_event
	placement = root

	title = burgundism.1.t
	desc = burgundism.1.d
	flavor = burgundism.1.f

	duration = 3

	trigger	= {
		has_technology_researched = totalism
		ig:ig_bgy_burgundists = { is_in_government = yes }
		has_law = law_type:law_ethnostate
		any_scope_state = {
			loyalist_fraction = { value >= 0.51 }
			is_incorporated = yes
			NOT = {
				has_decree = decree_genocide_suppression
			}
			any_scope_pop = {
				pop_is_discriminated = yes
				OR = {
					culture_accepted = no
					religion_accepted = no
				}
			}
		}
	}

	option = {
		name = burgundism.1.a

		add_modifier = {
			name = burgundistic_progroms
			months = very_short_modifier_time
		}
	}

}
